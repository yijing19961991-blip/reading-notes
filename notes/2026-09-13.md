# 2026-09-13

## 目标
为书架新增第二本书《从优秀到卓越》：按章节提炼全书，写每章不超过 1200 字的口播文字稿，参照《效率为王》数据结构编入书架，并推送上线。

## 完成
- 抽书：从 `从优秀到卓越.pdf`（280 页，pypdf）抽取全文到 `/tmp/g2g.txt`，按目录页页码＋正文起始行切分出 ch1–ch9（ch1 P8–23 … ch9 P202–223）到 `/tmp/g2g_ch/`，供逐章提炼。
- 通读 9 章原文，撰写 9 章口播文字稿（每章 507–704 字，全部 ≤1200），以《效率为王》同款 `notes[{h,items}]` 结构编入。
- data.js `books` 末尾新增 `g2g` 对象：书名/作者/封面色/标签/关键词/3 段全书概括 + 9 章（每章 tagline、keywords、音频占位 `null`、关键词版 mindmap、notes 口播稿）。
- 验证（全部本机完成）：
  - JXA（`osascript -l JavaScript`）语法校验 + 结构断言：2 本书、g2g 9 章、导图 `t/c` 齐全、audio 未设、notes 最长 704 字。
  - 修掉一处导图内层 ASCII 引号导致的 SyntaxError。
  - headless Chrome `--dump-dom`（本地 `python3 -m http.server`）：书架 2 本书、`#/book/g2g` 概况页、ch01–ch09 每章导图/文字提炼/音频口播占位均渲染无 JS 报错。

## 经验
- 口播稿直接复用 `notes`（`{h, items}`），无需新增字段：<1200 字的中文稿正是"文字提炼"；音频现录后填 `audio.src` 即可，界面自动由占位切换为播放器。
- data.js 是纯静态 JS（`window.READING_DATA = ...`）：改库后先跑一遍 JXA 语法+结构冒烟（不用装 node），再 headless Chrome 实测，比一上来就盯 DOM 快；今天就是靠 JXA 秒抓出"导图引号写成 ASCII"的 SyntaxError。
- Chrome 在 zsh 下带空格的路径要赋值到变量再执行（`CHROME='...'; "$CHROME" --headless=new ...`），否则 `zsh: no such file or directory: /Applications/Google`。

## 遗留 / 下一步
- 用户后续为 9 章补充音频 mp3 → `assets/audio/` + 相应 `audio` 字段。
- 可把"每章口播稿≤1200字"这条约束写进 AGENTS.md，作为以后写口播稿的固定规矩。